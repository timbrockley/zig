version: "3"

vars:
  CACHE_PATH: ".zig-cache"
  OUTPUT_PATH: "zig-out"

tasks:
  clean:
    cmds:
      - rm -rf {{.CACHE_PATH}} {{.OUTPUT_PATH}}

  build:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - zig build-lib ../libsqlite.zig -femit-bin=libsqlite.so -dynamic -L. -lsqlite3 -fPIC -O ReleaseSmall -target native -mcpu=baseline
      - patchelf --set-soname libsqlite.so libsqlite.so
      - zig build-exe ../main-sqlite.zig -femit-bin=main -dynamic -lc -fstrip -L. -lsqlite -O ReleaseSmall -target native -mcpu=baseline

  debug:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - zig build-lib ../libsqlite.zig -femit-bin=libsqlite.so -dynamic -L. -lsqlite3 -fPIC -O Debug -target native -mcpu=baseline
      - patchelf --set-soname libsqlite.so libsqlite.so
      - zig build-exe ../main-sqlite.zig -femit-bin=main -dynamic -lc -L. -lsqlite -O Debug -target native -mcpu=baseline

  run:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - ./main {{.CLI_ARGS}}

  test:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - ./main {{.CLI_ARGS}}
